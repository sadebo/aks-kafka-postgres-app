apiVersion: apps/v1
kind: Deployment
metadata:
  name: inventory-service
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: inventory-service
  template:
    metadata:
      labels:
        app: inventory-service
    spec:
      containers:
      - name: inventory-service
        image: aiinfraacr2901.azurecr.io/inventory-service:latest
        ports:
        - containerPort: 8000
        env:
        - name: POSTGRES_USER
          valueFrom:
            secretKeyRef:
              name: inventory-db-inventory_user.credentials.postgresql.acid.zalan.do
              key: username
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: inventory-db-inventory_user.credentials.postgresql.acid.zalan.do
              key: password
        - name: POSTGRES_DB
          valueFrom:
            secretKeyRef:
              name: inventory-db-inventory_user.credentials.postgresql.acid.zalan.do
              key: dbname
        - name: POSTGRES_HOST
          valueFrom:
            secretKeyRef:
              name: inventory-db-inventory_user.credentials.postgresql.acid.zalan.do
              key: host
        - name: POSTGRES_PORT
          valueFrom:
            secretKeyRef:
              name: inventory-db-inventory_user.credentials.postgresql.acid.zalan.do
              key: port
