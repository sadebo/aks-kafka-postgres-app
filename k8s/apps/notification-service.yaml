apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: notification-service
  template:
    metadata:
      labels:
        app: notification-service
    spec:
      containers:
      - name: notification-service
        image: aiinfraacr2901.azurecr.io/notification-service:latest
        ports:
        - containerPort: 8000
        env:
        - name: POSTGRES_USER
          valueFrom:
            secretKeyRef:
              name: notifications-db-notifications_user.credentials.postgresql.acid.zalan.do
              key: username
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: notifications-db-notifications_user.credentials.postgresql.acid.zalan.do
              key: password
        - name: POSTGRES_DB
          valueFrom:
            secretKeyRef:
              name: notifications-db-notifications_user.credentials.postgresql.acid.zalan.do
              key: dbname
        - name: POSTGRES_HOST
          valueFrom:
            secretKeyRef:
              name: notifications-db-notifications_user.credentials.postgresql.acid.zalan.do
              key: host
        - name: POSTGRES_PORT
          valueFrom:
            secretKeyRef:
              name: notifications-db-notifications_user.credentials.postgresql.acid.zalan.do
              key: port
