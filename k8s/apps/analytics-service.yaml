apiVersion: apps/v1
kind: Deployment
metadata:
  name: analytics-service
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: analytics-service
  template:
    metadata:
      labels:
        app: analytics-service
    spec:
      containers:
      - name: analytics-service
        image: aiinfraacr2901.azurecr.io/analytics-service:latest
        ports:
        - containerPort: 8000
        env:
        - name: POSTGRES_USER
          valueFrom:
            secretKeyRef:
              name: analytics-db-analytics_user.credentials.postgresql.acid.zalan.do
              key: username
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: analytics-db-analytics_user.credentials.postgresql.acid.zalan.do
              key: password
        - name: POSTGRES_DB
          valueFrom:
            secretKeyRef:
              name: analytics-db-analytics_user.credentials.postgresql.acid.zalan.do
              key: dbname
        - name: POSTGRES_HOST
          valueFrom:
            secretKeyRef:
              name: analytics-db-analytics_user.credentials.postgresql.acid.zalan.do
              key: host
        - name: POSTGRES_PORT
          valueFrom:
            secretKeyRef:
              name: analytics-db-analytics_user.credentials.postgresql.acid.zalan.do
              key: port
